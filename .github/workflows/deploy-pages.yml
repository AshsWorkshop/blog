name: 'Deploy to Github Pages'

on:
  workflow_run:
    workflows: 
      - Lock Blogpost Metadata
    types:
      - completed
  workflow_dispatch:

jobs:
  build:
    name: Build Docusaurus
    runs-on: ubuntu-latest
    
    steps:
      - uses: AshsWorkshop/actions/checkout@8ea65337160cab4672107cb767eff9b122b7b119 # main

      - uses: AshsWorkshop/actions/setup-node@8ea65337160cab4672107cb767eff9b122b7b119 # main
        with:
          node-version-file: '.node-version'

      - name: Install dependencies
        run: npm ci

      - name: Build website
        run: npm run build

      - name: Upload Build Artifact
        uses: AshsWorkshop/actions/upload-pages-artifact@8ea65337160cab4672107cb767eff9b122b7b119 # main
  
  deploy:
    name: Deploy to GitHub Pages
    needs: build

    permissions:
      pages: write
      id-token: write
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: AshsWorkshop/actions/deploy-pages@8ea65337160cab4672107cb767eff9b122b7b119 # main